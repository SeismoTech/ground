public class ${scheme.name} extends ${scheme.single} {

  private final ExceptionBundle<? extends ${scheme.single}> bundle;

  public ${scheme.name}(ExceptionBundle<? extends ${scheme.single}> bundle) {
    super(bundle.simpleMessage(), bundle.firstCause());
    this.bundle = bundle;
  }

  public ExceptionBundle<? extends ${scheme.single}> bundle() {
    return bundle;
  }
}
